// Check if the variable is available syntax ?. 
// If it is not available, the program will don't show error messages
obj = {
    ten : "Ngoc Anh",
    age : 19,
    getName(){
        console.log(x)
    }
}
// obj.getName?.[10]
// console.log(obj?.tenv)
let user = null;
let x = 0;
user?.sayHi(x+1); // no "user", so the execution doesn't reach sayHi call and x++

alert(x);